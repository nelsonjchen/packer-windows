dev-gce-kube:
  image: ubuntu:bionic
  script:
    - ls /dev
    - cat /proc/cpuinfo
    - uptime
    - apt -v
    - cat /proc/1/cgroup
  tags:
    - kubernetes

dev-gce-kube:
  image: ubuntu:bionic
  script:
    - ls /dev
    - cat /proc/cpuinfo
    - uptime
    - apt -v
    - cat /proc/1/cgroup
  only:
    - gitlab-org
