dev-gce-kube:
  image: ubuntu:bionic
  script:
    - ls /dev
    - uptime
    - apt -v
    - cat /proc/1/cgroup
  tags:
    - kubernetes

dev-gce-kube:
  image: ubuntu:bionic
  script:
    - ls /dev
    - uptime
    - apt -v
    - cat /proc/1/cgroup
  only:
    - gitlab-org
